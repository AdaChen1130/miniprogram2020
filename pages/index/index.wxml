
<view class="profile-container">
    <!-- 分类导航 -->
  <view class="nav-block wrap">
    <view wx:for="{{navItems}}">
      <view class="wrap-item ">  
        <navigator class="profile" url="../{{item.url}}/{{item.url}}" hover-class="navigator-hover">{{item.name}}</navigator>
      </view>
   </view>
  </view>
</view>

